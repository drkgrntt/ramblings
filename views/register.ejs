<% include ./partials/header %>

<div class="ui main text container segment very padded text">
    <div class="ui huge header">Sign Up!</div>
    <form class="ui form" action="/register" method="POST">
        <div class="field">
            <input type="text" name="username" placeholder="username">
        </div>
        <div class="field">
            <input type="password" name="password" placeholder="password">
        </div>
        <div class="field">
            <input type="password" name="adminCode" placeholder="for admin use only">
        </div>
        <button class="ui violet big basic button">Sign Up</button>
    </form>
</div>

<% include ./partials/footer %>